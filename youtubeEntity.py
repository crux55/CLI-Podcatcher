class YoutubeEntity:

    def __init__(self, url, folder, maxLength, maxRepeatedLength):
        self.url = url
        self.folder = folder
        self.mexLength = maxLength
        self.maxRepeatedLength = maxLength
